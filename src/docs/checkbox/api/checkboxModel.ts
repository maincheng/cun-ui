import { checkboxModel } from 'cun-ui';
import { convertProps } from '@/lib/utils';

export default {
    title: 'Model (checkbox)',
    columnsKey: 'model',
    orderNum: 1,
    data: convertProps(checkboxModel)
};
